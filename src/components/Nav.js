import '../styles/Nav.scss';

const Nav = () => {
    return (
        <nav className="kasa-nav">
            <a href="#">Accueil</a>
            <a href="#">A Propos</a>
        </nav>
    );
}

export default Nav;